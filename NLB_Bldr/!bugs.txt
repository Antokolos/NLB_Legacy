[1] Если для страницы не была определена переменная и страницу
copy/paste, то в новой странице будет имя переменной 0, 1, 2 
и т.п. Т.е. надо это проверять, и если у страницы нет собственной переменной,
то для скопированной тож не надо.
[2] pTrans = new CMathTrans(strTmpConstraint);					
				if (pTrans)
				{
					if (pTrans->Compile() != IERR_ERROR_SUCCESS) return FALSE;
Memory leak!!!
Это в Convert_To...
[3] Если содержимое строковой переменной не задано, то выводится некорректное
сообщение об ошибке.
[4] При переименовании линка куски старого текста остаются на экране.