1) Надо ли уничтожать иконку, созданную LoadIcon?
Если да, то надо подправить обработчик Build->Compile

2) Удалена такая строчка из OnMouseMove:
 m_fIsResizingNow = FALSE; - ЭТО ДЕЛАТЬ НЕЛЬЗЯ! БУДУТ БОЛЬШИЕ РАСХОДЫ (ПЕРЕРИСОВКА КАЖДЫЙ РАЗ ПОСРЕДСТВОМ PaintInMemory)

3) if (m_pDlgComperr) delete m_pDlgComperr;	
   if (m_pDlgSearchRes) delete m_pDlgSearchRes;
Вообще-то перед этим надо бы проверить, что соответствующие диалоги
закрыты. Однако, это, видимо, совершается автоматически, так как 
при попытке на OnDestroy для View вызвать DestroyWindow для 
диалога приводит к провалу...

4) // (***) -- пометим все же это место... Возможно, тут могут быть проблемы...
Проблемы могут быть с UNICODE

5) else if ((strCurPage[i]=='-') && _istspace((_TXCHAR) strCurPage[i+1]))
(Это в Convert_To_NLB_Engine_v10())
Данная конструкция, наверно, может неверно обрабатывать такое:
aaaaaaaa -
aaaaa
(после тире - не пробел, а \r)
Но вроде это все же работает. Это значит, что _istspace правильно определяет
\r как пробел? Я исправил, но все равно интересно...

6) Можно ли вызывать TrimLeft() и TrimRight() класса CString без параметров?
7) Проблема - бесконечная рекурсия в вызовах строковых функций. Аналогично
InfiniteLoop в переменных задачи, но это гораздо труднее проверить.
Пока это никак не проверяется, и пользователь вполне может устроить бесконечную
рекурсию в StringVariables.
IDS_STRERR_SELFREFERENCE_FOUND - обратить внимание и на этот вопрос
8) Если еще не исправил - редактирование ошибочных String Variables в том
случае, если в соответствующем MessageBox в ConvertTo... нажата кнопка Yes.
9) MoveUp - MoveDown в StringVariableProp-s
10) CList<...> lst;
lst.Serialize(ar);
ТАК ДЕЛАТЬ НЕЛЬЗЯ! СПИСКИ НАДО СОХРАНЯТЬ ПОКОМПОНЕНТНО! ПРОВЕРИТЬ!